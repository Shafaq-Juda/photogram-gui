<h1><%= @the_user.username%></h1>

<dl>
    <dt>ID</dt>
    <dd><%= @the_user.id %></dd>

    <dt>Edit user</dt>
    <dd>
        <form action = "/users/:path_username">
            <label for = "user_name">Username</label>       
            <input id = "user_name" name = "user_name" value = "@the_user.username"/>
            <button type = "submit">Update user</button> 
        </form>
    </dd>
</dl>

<h2>Own photos (photo count goes here)</h2>

<table border = "1">

</table>
